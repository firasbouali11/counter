class Workout {

  final int workoutDuration;
  final int lapsCount;
  final int restDuration;
  final int restLapsDuration;
  final int count;

  const Workout(this.workoutDuration, this.restDuration, this.lapsCount, this.restLapsDuration, this.count);
}